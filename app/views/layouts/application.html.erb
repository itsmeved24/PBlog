<!DOCTYPE html>
<html>
  <head>
    <title>PBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <link rel="stylesheet" href="<%= asset_path('custom.css') %>">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
  </head>

  <body class="<%= body_layout_class %>">
    <header class="header">
      <a href="/" class="logo">
        <span class="logo-icon">P</span>Blog
      </a>
      <div class="header-actions">
        <% if user_signed_in? %>
          <%= link_to 'New Post', new_post_path, class: 'animated-btn' %>
          <%= link_to edit_user_registration_path, class: 'social-icon' do %>
            <i class="bi bi-person-circle"></i>
          <% end %>
        <% else %>
          <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
        <% end %>
      </div>
    </header>

    <% if notice || alert %>
      <div class="flash-messages">
        <% if notice %>
          <p class="alert alert-success"><%= notice %></p>
        <% end %>
        <% if alert %>
          <p class="alert alert-danger"><%= alert %></p>
        <% end %>
      </div>
    <% end %>

    <div class="page-container">
      <main class="main-content-area">
        <%= yield %>
      </main>

      <footer class="footer">
        <a href="https://www.linkedin.com/in/vedank-vansia-73167b270" target="_blank" class="social-icon"><i class="bi bi-linkedin"></i></a>
        <div class="footer-thanks">Made with ♥ by Vansia!</div>
        <a href="https://github.com/itsmeved24" target="_blank" class="social-icon"><i class="bi bi-github"></i></a>
      </footer>
    </div>
  </body>
</html>